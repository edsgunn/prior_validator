"""Counterfactual editor for Concordia trajectories.

Replaces the focal agent's statement at a chosen turn with a better or worse
alternative generated by the LLM. Since Concordia games have complex state
dependencies, we use a lightweight approach:

- Generate a replacement statement at a different quality level.
- Keep the subsequent observation tokens unchanged (NPC responses and GM
  narration are held fixed), since full replay would require re-running the
  entire LLM-powered simulation.
- quality_delta is estimated from the quality score difference between the
  focal agent's quality level and the counterfactual level.

This gives a signal about whether the model's surprise tracks the quality of
the focal agent's statements, even without full causal propagation.
"""

from __future__ import annotations

import os
import random
from typing import Optional

from ccsm_eval.environments.base import CounterfactualEditor
from ccsm_eval.trajectories.base import CounterfactualEdit, Trajectory
from ccsm_eval.trajectories.concordia.agent_factories import _PERSONAS, make_agent
from ccsm_eval.trajectories.concordia.language_model import LanguageModel, make_language_model

# Estimated quality deltas between levels (ordinal scale)
_QUALITY_ORDER = ["random", "poor", "moderate", "expert"]
_QUALITY_SCORE = {q: i / (len(_QUALITY_ORDER) - 1) for i, q in enumerate(_QUALITY_ORDER)}


class ConcordiaCounterfactualEditor(CounterfactualEditor):
    """Generates counterfactual edits for Concordia focal agent statements.

    Args:
        model: Model ID (e.g. "claude-sonnet-4-20250514" or "gpt-4o").
        provider: API provider — "anthropic" (default) or "openai".
        api_key: API key. Falls back to ANTHROPIC_API_KEY / OPENAI_API_KEY env vars.
    """

    def __init__(
        self,
        model: str = "claude-sonnet-4-20250514",
        provider: str = "anthropic",
        api_key: Optional[str] = None,
    ) -> None:
        self._model_id = model
        self._provider = provider
        self._api_key = api_key
        self._model: Optional[LanguageModel] = None

    def _get_model(self) -> LanguageModel:
        if self._model is None:
            self._model = make_language_model(
                provider=self._provider,
                model=self._model_id,
                api_key=self._api_key,
            )
        return self._model

    def sample_edit_positions(self, trajectory: Trajectory) -> list[int]:
        """Return indices of focal agent action tokens, excluding first and last."""
        action_positions = [
            i
            for i, tok in enumerate(trajectory.tokens)
            if not tok.is_observation
        ]
        if len(action_positions) >= 3:
            return action_positions[1:-1]
        if len(action_positions) >= 2:
            return action_positions[:1]
        return action_positions  # fallback: return all if only 1

    def edit(
        self, trajectory: Trajectory, position: int, direction: str
    ) -> CounterfactualEdit:
        """Replace focal agent action at position with a better ("up") or worse ("down") alternative.

        The replacement is generated by prompting an LLM with the context
        leading up to that turn and the appropriate quality persona.
        """
        token = trajectory.tokens[position]
        if token.is_observation:
            raise ValueError(f"Token at position {position} is not an action token.")

        original_action = token.text
        scenario_name = trajectory.metadata.get("scenario_name", "resource_division")
        quality_level = trajectory.metadata.get("quality_level", "moderate")

        # Determine counterfactual quality level
        cf_quality = _counterfactual_quality(quality_level, direction)
        quality_delta = _quality_delta(quality_level, cf_quality, direction)

        # Build context from preceding tokens
        context_lines: list[str] = []
        for tok in trajectory.tokens[:position]:
            if tok.is_observation:
                context_lines.append(f"[Observation] {tok.text}")
            else:
                context_lines.append(f"[Action] {tok.text}")

        context = "\n".join(context_lines[-8:])  # last 8 events for context
        persona = _PERSONAS.get(scenario_name, {}).get(cf_quality, "You are a participant.")

        prompt = (
            f"{persona}\n\n"
            f"Here is the conversation so far:\n{context}\n\n"
            f"Now you (the focal agent) must respond. "
            f"Original response was: '{original_action}'\n"
            f"Generate a {'better' if direction == 'up' else 'worse'} response that a "
            f"{'skilled' if direction == 'up' else 'poor'} player would give. "
            f"Respond in 1-3 sentences."
        )

        model = self._get_model()
        rng_seed = hash((trajectory.trajectory_id, position, direction)) % (2**31)
        rng = random.Random(rng_seed)

        replacement_action = model.sample_text(prompt, max_tokens=200, temperature=0.5)
        replacement_action = replacement_action.strip()

        # Following observation tokens (GM narration, other agents, outcome)
        following_obs: list = []
        for tok in trajectory.tokens[position + 1:]:
            if not tok.is_observation:
                break
            following_obs.append(tok)

        return CounterfactualEdit(
            trajectory_id=trajectory.trajectory_id,
            edit_position=position,
            original_action=original_action,
            replacement_action=replacement_action,
            direction=direction,
            quality_delta=quality_delta,
            original_tokens=list(following_obs),
            replacement_tokens=list(following_obs),  # held fixed (no replay)
        )


def _counterfactual_quality(base_level: str, direction: str) -> str:
    """Get the counterfactual quality level for an edit direction."""
    idx = _QUALITY_ORDER.index(base_level) if base_level in _QUALITY_ORDER else 1
    if direction == "up":
        cf_idx = min(idx + 1, len(_QUALITY_ORDER) - 1)
    else:
        cf_idx = max(idx - 1, 0)
    return _QUALITY_ORDER[cf_idx]


def _quality_delta(base_level: str, cf_level: str, direction: str) -> float:
    """Estimate quality delta as a signed score difference."""
    base_score = _QUALITY_SCORE.get(base_level, 0.5)
    cf_score = _QUALITY_SCORE.get(cf_level, 0.5)
    delta = cf_score - base_score
    # Ensure sign matches direction
    if direction == "up" and delta <= 0:
        delta = 1.0 / (len(_QUALITY_ORDER) - 1)
    elif direction == "down" and delta >= 0:
        delta = -1.0 / (len(_QUALITY_ORDER) - 1)
    return round(delta, 4)
